-- General configuration for Hyprland

hl.config({
    general = {
        -- Borders and Gaps
        border_size = 0,
        gaps_in = 5,
        gaps_out = 10,
        
        -- Colors
        col = {
            active_border = mauve,
            inactive_border = blue,
        }
    },
    cursor = {
        sync_gsettings_theme = true
    }
})
